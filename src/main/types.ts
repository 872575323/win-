/**
 * 沉浸式隐蔽小说阅读器 - 类型定义（从共享模块 re-export）
 */
export {
  type WindowManagerConfig,
  type AppState,
  type ShortcutBinding,
  type ShortcutConflict,
  type PurifyRule,
  IPC_CHANNELS,
  DEFAULT_SHORTCUTS,
  WEREAD_PURIFY_RULES,
} from '../shared/types';
